import SwiftUI

struct FavoritesView: View {
    let favoritesViewModel = FavoritesViewModel.shared
    var body: some View {
        ZStack {
            AppBackground()
            
        }
    }
}
